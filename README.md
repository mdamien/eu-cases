# EU Competition cases

Extract all the cases from http://ec.europa.eu/competition/elojade/isef/index.cfm

To use, install dependencies (`pip3 install -r req.txt`) and then execute the scripts in order.

## TODO

 - [ ] Use the export feature instead of slowly crawling page by page
